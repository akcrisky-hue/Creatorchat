import { db, json, requireSession, requestId } from '../../lib.mjs';
export default async function handler(req,res){const rid=requestId(req);if(req.method!=='GET')return json(res,405,{error:'Method not allowed'},rid);try{const s=requireSession(req,res,[],rid);if(!s)return;const q=await db().query(`SELECT id,role,name,email,mobile,status,created_at AS "createdAt",updated_at AS "updatedAt" FROM users WHERE id=$1 LIMIT 1`,[s.id]);if(!q.rowCount)return json(res,404,{error:'User not found'},rid);return json(res,200,{user:q.rows[0]},rid);}catch{return json(res,500,{error:'Server error'},rid);}}
